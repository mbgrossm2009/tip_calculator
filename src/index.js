import React from 'react';
import ReactDOM from 'react-dom/client';
import TipCalculator from './Components/Form';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TipCalculator />
  </React.StrictMode>
);
